source :rubygems

# No gems in the default group...

group :test do
  gem "rspec", "= 2.0.0.beta.20"
  gem "lstrip-on-steroids", require: "lstrip_on_steroids"
end

group :install_only do
  gem "autotest"
  gem "autotest-fsevent"
end